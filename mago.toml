php-version = "8.0"

[source]
paths = ["includes", "openmira.php"]
includes = ["vendor/php-stubs/wordpress-stubs", "vendor/wordpress/mcp-adapter/includes", "stubs"]
excludes = [".git/**"]

[formatter]
print-width = 120
